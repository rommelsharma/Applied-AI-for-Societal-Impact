# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.4

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# Audio I/O and processing
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0

# Broadcast-grade post-processing (v2.0)
pyloudnorm>=0.1.1        # EBU R128 / ITU-R BS.1770-4 loudness measurement
noisereduce>=3.0.2        # Spectral subtraction noise reduction

# TTS engines
# NOTE: Install PyTorch separately via setup.sh — picks CUDA / MPS / CPU build
# kokoro and TTS list torch as a dependency; ensure torch is installed first.
kokoro>=0.9.4
# Coqui XTTS v2 — multilingual zero-shot voice cloning (17 languages incl. Hindi + Japanese)
# License: Coqui Public Model License v1.0 (commercial use ≤ $1M/yr revenue)
TTS>=0.22.0
# Coqui TTS requires transformers<4.43 — isin_mps_friendly removed in 4.43, BeamSearchScorer removed in 4.57
transformers==4.40.2
